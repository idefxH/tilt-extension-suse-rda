# Local dev: include the extension directly so changes to ../../suse_rda/Tiltfile
# are picked up without going through Tilt's repo-fetch cache.
include('../../suse_rda/Tiltfile')

suse_app(
    name='hello',
    language='nodejs',
    port=8080,
    # services= defaults to None → auto-discovered from chart/values.yaml
    # (suse-library.services[] DSL + legacy <chart>.enabled).
)
